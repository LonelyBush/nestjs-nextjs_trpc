{
  "name": "nestjs-nextjs-trpc",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "dev:api": "pnpm --filter api run start:dev",
    "dev:web": "pnpm --filter web run dev",
    "build": "pnpm -r run build",
    "trpc:generate": "pnpm --filter api run trpc:generate"
  }
}
